Create Folder src and File index.js

npm init -y

//Create webpack local(It will crate Folder "dist")
npx webpack 
npx webpack --mode development
npx webpack --mode production


// Tuto project
npx webpack --mode production --config webpack.config.js
